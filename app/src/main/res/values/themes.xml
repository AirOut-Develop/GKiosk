<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. No title/action bar. -->
    <style name="Theme.GKiosk" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Hide title/action bar -->
        <item name="windowNoTitle">true</item>
        <item name="windowActionBar">false</item>

        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/primary_main</item>
        <item name="colorPrimaryVariant">@color/primary_darker</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Controls (Switch/Checkbox/Radio) active color -->
        <item name="colorControlActivated">@color/primary_main</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_1</item>
        <item name="colorSecondaryVariant">@color/teal_0</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">?attr/colorPrimaryVariant</item>
        <!-- Global font family -->
        <item name="android:fontFamily">@font/pretendard</item>
        <item name="fontFamily">@font/pretendard</item>
    </style>

    
</resources>
